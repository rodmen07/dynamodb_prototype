[package]
name = "dynamodb_prototype"
version = "0.1.0"
edition = "2024"

[dependencies]
aws-config = "1.5"
aws-sdk-dynamodb = "1.56"
tokio = { version = "1.37", features = ["macros", "rt-multi-thread"] }
